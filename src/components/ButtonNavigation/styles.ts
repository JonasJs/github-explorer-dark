import styled from 'styled-components';

export const Container = styled.div`
  display: flex;

  cursor: pointer;
  p,
  svg {
    color: var(--gray);
  }
  p {
    font-size: 16px;
    font-weight: bold;
  }
`;
